import { Customer } from "@/types/customer";

/**
 * PRODUCTION RESET: Cleared all hardcoded customer data.
 * The system now defaults to an empty state ready for first-time use.
 */
export const MOCK_CUSTOMERS: Customer[] = [];
