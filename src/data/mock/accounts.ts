import { GLAccount } from "@/types/account";

/**
 * PRODUCTION RESET: Cleared all hardcoded Chart of Accounts data.
 * Ready for fresh company setup.
 */
export const MOCK_ACCOUNTS: GLAccount[] = [];
